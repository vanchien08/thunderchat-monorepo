syntax = "proto3";

package search;

import "google/protobuf/empty.proto";

service ElasticSearchService {
  rpc SyncDataToES(DataToSync) returns (google.protobuf.Empty);
}

message DataToSync {
  string type = 1;
  oneof data_to_sync {
    DataToSyncObject dataObject = 2;
    DataToSyncArray dataArray = 3;
  }
}

message DataToSyncObject {
  string data_json = 1;
}

message DataToSyncArray {
  repeated int32 data = 1;
}